#pragma once

enum Action {
	MOVE_RIGHT,
	MOVE_LEFT,
	MOVE_FRONT,
	MOVE_BACK,
	DELETE_BLOCK,
	BUILD_BLOCK,
	QUIT,
	CULL
};

enum State {
	MOVING_FRONT,
	MOVING_BACK,
	MOVING_LEFT,
	MOVING_RIGHT

};

enum Range {
	LOOK_X,
	LOOK_Y
};